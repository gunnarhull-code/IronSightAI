# Copy this file to `.env` (git-ignored) before running the app or tests.
# The Flutter app loads `.env` at boot via flutter_dotenv (see lib/app/bootstrap.dart)
# and it is declared as a Flutter asset in pubspec.yaml, so `.env` MUST exist or the
# build/tests fail with "No file or variants found for asset: .env".
#
# The values below are the DEFAULT local Supabase credentials produced by
# `supabase start` (deterministic demo keys — safe for local dev only, never production).
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
